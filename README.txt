Analizamos las tasas de asesinatos por arma en Estados Unidos por estado.

El script "download-data.R" contiene el código usado para descargar los datos en csv.

El folder "data" contiene el archivo csv a partir del que se realiza el estudio.

El script "wrangle-data.R" contiene el código usado para crear el objeto en R para el estudio.

El folder "rdata" contiene el objeto .rda para usar el csv en nuestro estudio.

El script "analysis.R" contiene el código usado para hacer el estudio, este genera un gráfico.

El folder llamado "figs" contiene el archivo png generado por el script "analysis.R"